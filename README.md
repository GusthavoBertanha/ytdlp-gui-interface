# yt-dlp GUI Interface

A simple graphical interface for **yt-dlp**, designed to make video and audio downloads easier without using the command line. The application automatically manages required tools such as yt-dlp and ffmpeg, providing a practical and portable solution for Windows and macOS.

## Build commands

### Windows

```bash
python -m PyInstaller --noconsole --onefile yt_dlp_gui.py
````

### macOS

```bash
pyinstaller --windowed --onefile yt_dlp_gui.py
```

---

# Interface GUI para yt-dlp

Uma interface gráfica simples para o **yt-dlp**, criada para facilitar o download de vídeos e áudios sem precisar usar linha de comando. O aplicativo gerencia automaticamente o yt-dlp e o ffmpeg, oferecendo uma solução prática e portátil para Windows e macOS.

## Comandos para compilar

### Windows

```bash
python -m PyInstaller --noconsole --onefile yt_dlp_gui.py
```

### macOS

```bash
pyinstaller --windowed --onefile yt_dlp_gui.py
```
